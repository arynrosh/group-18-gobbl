# Leave unset to use the Vite dev proxy at /api (see vite.config.ts).
# For production or when calling the API directly, set the full origin:
# VITE_API_BASE_URL=http://127.0.0.1:8000

VITE_API_BASE_URL=
